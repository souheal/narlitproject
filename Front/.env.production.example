# Copy this file to `.env.production` and fill in the real production API URL.
# Next.js reads this file when building for production (`npm run build`).
NEXT_PUBLIC_API_BASE_URL=https://api.narlit.com/api/v1
